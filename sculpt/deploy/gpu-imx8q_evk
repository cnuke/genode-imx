config | arch:

+ common_routes
  + service ROM | label_last: ld.lib.so | + parent
  + service ROM | label_last: init      | + parent
  + service CPU                         | + parent
  + service PD                          | + parent
  + service LOG                         | + parent
  + service Timer                       | + parent

+ option managed

+ start gpu_test | pkg: glmark2-imx8q_evk
  + route
    + service Platform | + parent | label: gpu
    + service Gui      | + parent
-
